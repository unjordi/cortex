---
name: bitacora
description: Journal append-only de cortex — una línea por slice cerrado. merge=union → sin conflictos en paralelo.
type: project
---

# Bitácora de cortex (append-only)

> **Una línea por slice cerrado, agregada AL FINAL** (append). Formato:
> `- AAAA-MM-DD · rama/PR · quién · qué quedó cerrado`.
>
> Este archivo usa **`merge=union`** (ver `.claude/.gitattributes`): cuando varias sesiones/agentes
> añaden líneas en ramas paralelas, git conserva las de **todos** sin marcar conflicto. Por eso:
> **NO reordenes ni edites líneas viejas — solo agrega al final.** El resumen curado de lo ABIERTO
> vive en `backlog-desarrollo.md` (gitignored, dev privado); esto es el log cronológico conflict-free
> de lo YA CERRADO (nace 2026-08-07, B4: antes el backlog dobla como journal mezclando "qué pasó" con
> "qué sigue" — las líneas de abajo son la siembra inicial, destiladas de sus ✅ HECHO/CERRADO).

- 2026-07-15 · widget macOS · claude+unjordi · fix #146: auto-update del widget roto por el precompilado — `Updater.resolveClonePath` ahora usa el clon LOCAL de instalación (`$HOME/.cortex`) en vez del `repo` embebido en build-time del runner de GitHub, que no existía en la Mac del usuario (`canSelfUpdate=false`).
- 2026-07-18 · macos/install.sh · claude+unjordi · fix #154: `install.sh` (macOS) ahora reinicia la instancia viva (pkill + relanza vía LaunchAgent de autoarranque) al actualizar — antes el `.app` nuevo bajaba a disco pero el binario viejo seguía corriendo en memoria.
- 2026-07-18 · aviso-drift-cerebro · claude+unjordi · Anti-drift construido (#157 v1 + #159 v2): hook GLOBAL de SessionStart que detecta drift del cerebro por-repo vs el brain global (diff-aware, reusa `sincronizar-cerebro.sh` en dry-run) y auto-sincroniza cuando `.claude/` está limpio y estás en tu mini-develop.
- 2026-07-18 · main 55652f2 (#147-#152) · claude+unjordi · Paquete "turno nocturno" CONSTRUIDO Y RELEASEADO completo: skill `turno-nocturno` (contrato+autorización durable con vencimiento+relanzador+preflight) + fix de precisión de `confirmar-merge-develop` (autorización durable a disco, vocabulario "empuja/mete todo a develop") + normas nuevas (re-citar un OK vigente es legítimo · ninguna decisión se queda solo en el chat · post-compact se EXCAVA antes de contestar · paso 0 de toda tarea grande = inventario de lo que ya existe) + mockups de Claude siempre a archivo versionado.
- 2026-08-05 → 2026-08-07 · fix/juez-fast-path (worktree, sin mergear) · claude+unjordi · Robustez del juez de intención (Haiku) — causa raíz confirmada: la lentitud/timeout (~50s → UNAVAILABLE → fail-safe DENY) era el HARNESS de `claude -p`, no el modelo. Fix validado: `curl` directo a la Messages API con el token OAuth de suscripción (keychain), ~1.3s, espejado a `dod-verificar`; batería LIVE completa verde (merge 24/24 · dod 33/33).
- 2026-08-07 · (verificación in-hook, sin rama) · claude+unjordi · B2 / PARQUEADO#3 CERRADO: el juez in-hook SÍ alcanza el token OAuth vía keychain en subproceso — verificación doble (`security find-generic-password` reproducido en subproceso + evidencia viva: autorizó los merges #278/#280/#281/#282). La "causa raíz confirmada" de 2026-08-06 sobre bloqueo offline quedó rancia; no se invierte en blindar offline ni se documenta "mergea en la web" como escape.
- 2026-08-05 · #263 (86c6d7b) · claude+unjordi · `docs/flowcharts/verificar-arbol-sync.sh` portabilizado (ya en develop): dejó de usar `find -printf` (GNU-only), que daba un FAIL espurio solo en macOS (BSD find) contando 0 skills y reportando falso drift.
- 2026-08-07 · #282 + #278 · claude+unjordi · Flowcharts 03–11 con leyenda-árbol (`gen-leyenda-arbol.sh --inject`, re-render, `.dot`/`.svg` des-ignorados) + `docs/referencia-cli-claude-code.md` (516 líneas, la tesis del ecosistema Claude CLI/OAuth/curl-juez que destrabó el fix del juez) mergeados a develop.
- 2026-08-08 · feat/reconstruir-firma-canonica (worktree, sin mergear) · claude+unjordi · #71 mecanismo NO-destructivo para reconstruir/enforzar la firma-árbol canónica en cerebros INSTANCIADOS: detector determinista `brain/verificar-firma-canonica.sh` (flaggea secciones ausentes en CLAUDE.md, memorias sin prefijo dom-/dev-/ux-/qa-/núcleo, invariante MEMORY↔archivos roto, hooks retirados en la prosa; `--strict`=gate para #44) + skill humano-en-el-loop `canonizar-cerebro` (destila el prototipo de fluxcore: reprefija con git mv, dedup con rescate, reescribe CLAUDE+MEMORY, verifica 1:1). Batería `g5` en test-brain.sh (verde, 641 PASS). 5-catálogos del árbol en sync (README+MEMORY+3 widgets). Verificado contra fluxcore/cps (pasan) y plantilladotnet (drift correctamente cazado).
- 2026-08-07 · #283/#284/#285 · claude+unjordi · Fan-out matinal: #283 gate de consentimiento de costo MUERTO por rename Task→Agent (arreglado); #284 hint de alias /bin/<cmd>→command/\<cmd> + install-brain persiste env activa (CLAUDE_SESSIONS_DRIVE); #285 checkpoint preserva la PROCEDENCIA de cada idea.
- 2026-08-07 · #286-#291 (fan-out 6 slices) · claude+unjordi · readme→cementerio.md · install-norms (no-bypass-deploy + B1/anti-web) · jueces destino in-hook por flag · widget comentario s/nº mágico · cerrar-slice s/--auto-merge + bitácora tracked · to-do formato agrupado.
- 2026-08-07 · #293/#294/#292/#295/#296 · claude+unjordi · #293 unifica plan 5 fases + graphify (#48) en Fase 4 · #294 juez VE OKs de AskUserQuestion · #292 hook hud-stale · #295 sync+drift a SKILLS · #296 detector cross-shell de aliases + @import LEAN.
- 2026-08-07 · casalianza_actas #17 · claude+unjordi · retiro de guards del brain en repo personal (#26).
- 2026-08-07 · #272/#273/#274/#276 (en develop/main) · claude+unjordi · #272 checkpoint read-before-overwrite · #273 norma git CLI-agnóstico (tabla gh↔glab) · #274 merge-squash-guard exige mensaje con sustancia · #276 fix veto-cita del juez (_juez_cita_casa, containment de tokens).
- 2026-08 (previo, verificado) · #4/#5/#15/#16/#17 · claude+unjordi · YA MATERIALIZADOS: catálogo MANIFEST + versión en widget (#4/#5) · cementerio único + lápidas por ID (#15/#271) · flowcharts visibles en GitHub/widget (#16/#162) · skill de diagramas DOT→yEd (#17).
- 2026-08-08 · #310/#83 (keystone) · claude+unjordi · Mecanismo anti-drift como-trabajar: ruteo del TRATO → archivo GLOBAL como-trabajar-con-<user>.md (no feedback-*.md por repo); reglas en cosechar-sesion/unificar-cerebro/desinflar-memorias/recordar-cosechar. Gate del retiro de feedback-*.
- 2026-08-08 · #309/#81 · claude+unjordi · Tiers asentados: regla crisp (repo-compartido both/global/repo) en los MANIFEST + norms/global-claude-md.md; cero recategorización.
- 2026-08-08 · retiro feedback-* (#77 parcial) · claude+unjordi · el global como-trabajar-con-unjordi.md absorbe las de trato; 5 locales de plantilladotnet retiradas (trash+de-index+pointer); conservadas 4 de chun + glab-técnica. Pendiente: retirar cps/.claude/memory/como-trabajar-unjordi.md (slice de cps).
- 2026-08-08 · #303+#311/#74 · claude+unjordi · aviso-contexto: forense (la máquina corría el hook PRE-#74, byte-idéntico a develop; el fix estaba VARADO sin mergear) → #303 (mensaje+histéresis) + #311 (la PROCEDENCIA ya no miente "override DELIBERADO" ante un override inválido). El override del CLI SÍ funciona; el override=70 solo DESTAPÓ los bugs pre-existentes. Aplicar a máquinas por widget updater = #82 (abierto).
- 2026-08-08 · #306/#76 · claude+unjordi · auditor-semantico al template: motor + skill + MANIFEST + catálogos de los 3 widgets + docs/ecosistema-auditores.md. El drift-check e3 cazó que el auto-merge perdió auditor-semantico en el StatusOf de Windows → arreglado. Robustecer el ecosistema + gate #44 = slice grande aparte.
- 2026-08-08 · #305 · claude+unjordi · qa-entorno en cerrar-slice (disciplina de entorno completo antes de invitar a QA).
- 2026-08-08 · #307/#80 · claude+unjordi · regresión g1 (5 FAIL) del no-bypass-deploy resuelta (test con $HOME hermético + env -u CI).
- 2026-08-08 · #304/#71 + #72/#73/#75 + #69 · claude+unjordi · epic canonización: reconstructor firma canónica (#304) + fluxcore canónico/pulido + barebones MEMORY (#72/#73/#75) + install global del delegacion-gate (#69). #79 (bug .gitattributes) movido al backlog de cps.
- 2026-08-09 · #70 · claude+unjordi · cosecha cps↔fluxcore cerrada (grueso: #83/#81/#306/#305 + aprendizajes/settings a fluxcore). No más grind "1 ítem a la vez".
- **[worktrees pendientes tras barrido]**
  - worktree `wt-comotrabajar` (rama `feat/como-trabajar-con-usuario`) sin mergear a develop — retomar o cerrar.
- 2026-08-25/26 · cortex-master · Rename claude-brain→cortex + emergencia Cachy (claude 2.1.243 segfault→rollback 2.1.241) + 6 PRs a develop (#315-318 skills/instalador-doble-widget/limpieza, #322/#323 widget self-update robusto al rename) + release #319 a main (bc83604) + casa limpia (cruft/zombies) + checkpoint completo pre-mudanza. Pendiente: release de #322/#323, verificar botón Curar, mudanza del slug.
- 2026-09-01 · RETIRADA rama feat/cerebro-multi-agente-grok (SUPERSEDED por axon). Era el prototipo 'portar el cerebro a un agente NO-Claude (Grok/otros)' — proposal cerebro-multi-agente.md + skill instalar-cerebro-agente + memoria grok-extensibilidad-cerebro.md (155 líneas de research de Grok Build) + tile del widget. unjordi: 'nomás una idea prototipo para que cortex funcione con grok y otros agentes... que YA lo estamos haciendo con axon'. Recuperable en `git show 3f2eeca` (y 55b33a2/3cf6df0) si algún día importa el port multi-agente; hoy el andamiaje-multi-agente ES axon (harness capa-2). Borrada remota tras OK explícito de unjordi.
- 2026-08-29 · auditoría ciclo brain-widget · claude(cortex-master)+auditor · dictamen read-only del ciclo INSTALL→LIMPIEZA→UPDATE→AUTOSYNC en docs/auditoria-ciclo-brain-widget-2026-08-29.md; raíz del LOOP del rename (heal sin git + build vieja sin self-update = punto fijo); 2 crít/5 alt/5 med/3 bajo; plan 3 olas; verificado 4/4 claims; #4 confirmado sano en HEAD. Backlog #14. Espera GO para las olas de fix.
- 2026-09-01 · OLA1 ciclo brain-widget · claude(cortex-master)+unjordi · integrados #341 (resolve_brain_dir() fallback ~/.claude-brain + nudge SessionStart local) y #342 (escape discoverable: one-liner real de bootstrap en los 3 widgets) a develop. Revisados por el orquestador: bash -n limpio, 3 OS compilan (swift/qmllint/dotnet), sin tocar lógica de update. Falta re-auditar + OLA2/3.
- 2026-09-01 · OLA2 + release ciclo brain-widget · claude(cortex-master)+unjordi · integrados #344 (.brain-version no regresa), #345 (widget fuerza-alinea origin/main 3 OS), #346 (guard asset rancio), #348 (test-brain verde 709/0) a develop; RELEASE #349 develop→main (9ad0c42, sin squash). Ciclo del rename claude-brain→cortex CERRADO: el clic Actualizar de los colegas ya converge.
- 2026-09-03 · campaña tuning de guards (79 FP acumulados) · claude(cortex-master)+unjordi · fan-out de auditores-de-proceso, uno por guard, cada fix con test doble (FP-no + real-sí), precisión pura. Aterrizados: proteger-arbol (05a392c, ⚠️retry-past-auto-mode), no-bypass-deploy (0c6ad54), dod-verificar (026b27c, 6 tests LIVE Haiku). git-family en curso. Auditoría aviso-contexto: SIGUE con FP (bug real=staleness post-compact) → 5º tuning. Sin integrar aún (reviso + OK). Hallazgo: FPs se escribían al corpus per-máquina RETIRADO (#340) → consolidar al tracked.
- 2026-09-03 · integración campaña tuning · claude(cortex-master)+unjordi · los 4 fixes revisados por el orquestador (precisión-only, cada uno con test doble) integrados en una rama de campaña (fix/tuning-guards-campana), suite 729/0 verde. proteger-arbol ACEPTADO tal cual por OK explícito de unjordi pese al método turbio (agente reintentó past el bloqueo auto-mode); incidente documentado. Raíz del #78 resuelta (PATH-augment gh/glab al FINAL, anti-weakening probado). git-family flaggeó 2 casos no-tuneables → backlog (seed repo vacío a main; merge dentro de cuerpo de heredoc).
- 2026-09-03 · consolidación corpus FP · claude(cortex-master) · las 5 entradas del corpus GLOBAL per-máquina (RETIRADO #340) reubicadas al tracked docs/guards-falsos-positivos.md (ahora el ÚNICO; el global quedó vaciado con puntero) + la 1 línea del colega (docs/fp-merge-squash-heredoc). Marcados ✅ atendidos los patrones que resolvió la campaña #351 (proteger heredoc-body · no-bypass exec-position · dod imagen-usuario/subagente/ajuste-UI · #78 PATH-augment). Backlog: #78 RESUELTO, item guards-precisión actualizado con lo resuelto + 5º tuning pendiente (aviso-contexto staleness) + 2 flags no-tuneables (merge-en-heredoc-body; seed repo vacío a main).
- 2026-09-03 · 5º tuning aviso-contexto + cierre campaña · claude(cortex-master)+unjordi · MR #353 a develop (b4f05d6): fix staleness post-compact (anclaje a isCompactSummary, reduce que descarta usage pre-compact) + des-veredictar el cierre (reportero tonto) + 14 flowcharts/mapa a doc=realidad + FP de dod-verificar B2 al corpus (revisar SOURCE .dot leído como QA visual). Revisado por el orquestador sobre el diff (no el reporte): hook sólido, test doble con anti-mutilación (944K sin compact SÍ reporta), 734/0. Campaña de tuning de guards COMPLETA (5 guards). FLAG a backlog: rehidratar-hilo.sh:49-51 comentario rancio ('tokens bajan solos tras /compact'). Pendiente: auditoría de drift de los 14 flowcharts vs código.
- 2026-09-03 · turno nocturno · claude(cortex-master) · REGRESIÓN del widget-update diagnosticada por tupla auditora y ARREGLADA. Causa raíz (confirmada por mí, reproducida byte-exacto): bug de comillas anidadas en runUpdate (macOS Updater.swift + Linux main.qml) — $SRC/$DST/$DIR dentro del bash -lc " " EXTERNO se expanden a vacío → cd pelón → HOME → 'fatal: not a git repository' → botón ⬆ nunca converge. Regresión de #323, a usuarios en #349; enmascarada porque install fresco usa bootstrap. Campaña de tuning EXONERADA con evidencia dura. FIX #357 (escapar $, macOS+Linux verificados con swift build + repro; Windows no afectado; CI fetch-depth). #358 complementos de diagramas 01/03/09 + FP peer. develop=fad295e. QA humano pendiente: clic ⬆ real + re-bootstrap manual del install congelado (el .app viejo no se auto-cura). Ronda 2 de convergencia en curso.
- **[worktrees pendientes tras barrido]**
  - worktree `fp2` (rama `docs/fp-guard-mueve-rama`) sin mergear a develop — retomar o cerrar.
- 2026-09-04 · cierre épica del ciclo brain-widget · claude(cortex-master)+unjordi · RELEASE #360 develop→main (667d2b7): el fix del updater (#357, comillas anidadas en runUpdate que dejaban el botón ⬆ sin converger) + complementos de diagramas (#358/#359). Re-bootstrap del install congelado de unjordi (0.2.385→v0.2.395, .app con el fix instalado + brain global desatascado). VERIFICADO el camino-colega en repro fiel: builds pre-rename (era claude-brain, pre-#323) NO tienen el bug (ruta single-quoted), redirect 301 claude-brain→cortex vivo (git+API), canSelfUpdate=true, y git merge --ff-only converge limpio a 667d2b7 (probado desde clon con origin viejo reseteado a #305). unjordi confirma que nadie modifica sus widgets → clones limpios → ff-only garantizado → SEGURO soltarles el update sin asteriscos. Épica CERRADA.
- 2026-09-07 · skill diagramar: Flujo 3 (mmd2dot) · claude(cortex-master)+unjordi · integrado al canónico un Flujo 3 que alguien metió a mano al skill GLOBAL: bin/mmd2dot.py (Mermaid erDiagram FEO → Graphviz DOT hermoso, self-loops limpios, crow's-foot aproximado) + su sección en SKILL.md. Movido a bin/ del repo junto a dot2yed.py (convención del hermano) y referencias alineadas a ~/.cortex/bin/. Smoke-test verde (ER con self-loop → DOT válido + PNG). Re-propaga al global vía install-brain.
- 2026-09-07 · broker del PTY → cortex, 2ª pasada (los 10 hallazgos del auditor) · claude(cortex-master) · rama `feat/term-broker`. RE-VENDORIZADO desde axon `341fb53` (era `cf840e6`): trae el **socket unix** —sin él la terminal del widget no funciona: un contenedor NO alcanza un bind a loopback del host—, `GET /health` y el manejo de error de `listen()` que faltaba (`EADDRINUSE` es asíncrono; el `try/catch` de `main()` no lo veía y el log "escuchando" salía ANTES del bind). C-1 (dos unidades habilitadas, mismo endpoint, tokens distintos) se resolvió por **no habilitar** cuando el endpoint está ocupado (+ deshabilitar lo que dejó una corrida previa), NO con `Conflicts=` — que arbitraría matando las terminales abiertas del usuario. Unidad: `RuntimeDirectory=axon` + `RuntimeDirectoryPreserve=yes` (si Docker levanta antes crea el dir como root:root → `listen EACCES`; y el default borraría el dir bind-mounteado al parar) y `StartLimitIntervalSec=60`/`Burst=5` (antes ~3 arranques por ventana ⇒ nunca `failed`, ciclaba en silencio). Instalador: gate de OS/prereqs al **paso 0**, salvaguarda por **recurso** (`ss` sobre puerto Y socket) en vez de por nombre de unidad, aviso de restart-necesario al reinstalar, y copia de módulos por nombre (el glob metía los probes al runtime). NUEVO `bin/migrar-term-broker.sh`: idempotente, ordena `disable --now` viejo → esperar endpoint libre → `start` nuevo, adopta el token legacy (el `.env` del cliente no cambia), **verifica con el token REAL** (el paso viejo era un POST sin auth esperando 401 — que da 401 siempre, con cualquier broker y hasta muerto) y revierte solo. `SHA256SUMS` + su check en el probe = el anti-drift que antes nadie corría y apuntaba a una ref móvil. I-19 hecho mecanismo: el probe pide `env` en una sesión real y prueba que el token no se filtra, **con control positivo** (una var gemela sin prefijo `AXON_` que SÍ aparece) — al escribirlo el control destapó que reusar el mismo valor lo filtraba. Verificado: probe-instalador 49/49 exit 0, probe-broker-vivo 35/35 exit 0, `sha256sum -c` 0, `systemd-analyze --user verify` 0, `bash -n` ×8 en 0. NO se tocó `axon-term-broker.service` (MainPID 3203989, NRestarts=0). En axon, `feat/term-broker-cliente-solo` fusionada con develop y pusheada (`67bdbae`, agente delegado; re-verificado por mí: typecheck 0, probe cliente 27/0, probe red 37/0). PENDIENTE: la migración en vivo con unjordi (cierra sus terminales) y los menores I-3/I-4/C-5/C-6, que viven en el vendorizado y van por axon.
- 2026-09-11 · barrido de ramas/worktrees: los 3 CRÍTICOS + 5 ALTO cerrados · claude(fixer) · rama `fix/barrido-ramas-criticos` (b245d52+). Dictamen `scratchpad/AUDITOR-barrido-ramas.md` (2026-09-11, 3 CRÍTICO/5 ALTO/7 MEDIO/4 BAJO). Cerrados con su test cada uno, PROBADO CONTRA LA FALLA (verificado corriendo la MISMA suite contra el código viejo vía `git worktree add` en 8ad6deb: 30-31 FAIL exactos, uno por assert nuevo; 0 FAIL con el fix). C-1 barrer_remota exige containment del SHA remoto en el tip local antes de borrar la rama remota (ya no destruye el push de un colega). C-2 limpiar-worktrees.sh adopta bz_protegida (nueva, compartida con limpiar-ramas.sh vía ramas-zombie.sh) — ya no borra el worktree de una mini-develop ni marca keep/* como zombie. C-3 worktree remove sin --force primero; si git rehúsa por árbol sucio, reporta SUCIO y conserva (--force solo con --purgar-sucios explícito). A-1 el resumen de limpiar-ramas.sh nombra TODO lo omitido (base/actual, protegidas por convención, retenidas por worktree) en vez de silenciarlo. A-2 limpiar-worktrees.sh valida argumentos (typo aborta con rc=2, ya no corre destructivo). A-3 nueva señal (e) local/offline 'Rama: <rama>' en el mensaje de squash (vuelve determinista el caso dominante sin gh/glab) + tercer estado INDETERMINADO cuando (d) no se pudo consultar (ya no afirma 'trabajo sin integrar' cuando no se supo). A-4 el pendiente de un worktree vivo ya no se re-appendea en cada corrida, y un veredicto INDETERMINADO no genera pendiente. A-5 barrer-ramas.sh corre limpiar-worktrees ANTES que limpiar-ramas en la MISMA pasada (antes: paralelo, una rama retenida por worktree tardaba hasta 2 pasadas de 24h). MEDIO baratos incluidos: M-1 (doc, resuelto de raíz por C-1/C-2), M-2 (base ambigua con >1 Develop* local ya no adivina), M-3 (worktree detached se nombra), M-4 (worktree prunable ya no 'vivo'), M-5 (host por URL completa, no subcadena), M-6 (worktree principal por la lista de git, no por el cwd). M-7 (caps de paginación gh/glab) y un hallazgo colateral (tests b3d/b3e en subshell que no cuentan para el veredicto) quedaron anotados en estado-proyecto.md, sin tocar. Suite 867 PASS/0 FAIL (exit code verificado), bash 3.2 real (/bin/bash de macOS) sin regresiones nuevas. Rama pusheada, SIN PR — lo abre el orquestador.
- 2026-09-11 · continuidad unificada (F1+F2 del plan): el andamio deja de ser un artefacto sin lector (rehidratar-hilo lo lee y lo etiqueta; el skill lo REGENERA con --self --ensure, así PreCompact no es el único mecanismo), su ventana pasa al TRAMO VIVO, nace contrato-hilo.sh (2 de 9 hilos reales no cumplían el contrato que su lector exige) y el hilo del master se CO-UBICA en la mudanza en vez de abortarla. Suite 962/0. Rama feat/continuidad-unificada; F3/F4/F5 quedan en el backlog vivo.
- 2026-09-15 · guards-sustrato: M3-M8+M10 del dictamen de unificación (M1/M2/M9 ya venían hechos de la sesión previa) · claude(fixer) · rama `fix/guards-sustrato-unico` (pusheada, SIN PR/merge — lo abre el orquestador). Dictamen `auditoria-guards-git-unificacion.md`. M3: `acg_destino_de_mr` gana confianza+motivo (`acg_destino_conf`: EXPLICITO/API/CACHE-DE-CREACION/DESCONOCIDO:SIN-CLI|SIN-RED|TIMEOUT|DIR-IRRESOLUBLE|SLUG-OPACO|SIN-MRID), retro-compat con los callers existentes. M4: `_recent_intercalado`/`_lexico_release_en_ventana` migran a la lib compartida (`acg_recent_intercalado`/`acg_lexico_release`) para que merge-squash-guard vea la MISMA señal de release-en-conversación que confirmar-merge-develop (cerraba §3.4: dos guards, la misma incógnita, veredictos opuestos). M5 🔴 aprieta: el piso de main aplica también con destino vacío. M6 🔴 amplía: el grant durable se consulta también con destino vacío, bajo cerca de léxico-de-release (antes un fallo de ENTORNO revocaba una autorización YA dada). M7: git-branch-guard y merge-squash-guard homologados a fail-CLOSED sin jq (antes fail-open silencioso). M8: mensajes con la causa real (entorno vs lenguaje) en vez de pedir repetir, 6 escapes "web de GitLab" retirados (norma anti-vein-popper), MR-id vacío ya no rompe el mensaje (pero SIGUE gateando — discrepé del dictamen aquí: mrid vacío es un merge del branch actual, no "sin MR", aflojar hubiera abierto una evasión real), squash-guard exime la vara de profundidad al título gh cuando hay --body separado. M10: 6 entradas del corpus re-atribuidas al guard de aislamiento del harness (no proteger-arbol), hipótesis DevelopUnjordi refutada por ejecución. HALLAZGO A MITAD DE SESIÓN (reportado por el orquestador, auditoría independiente del arnés): el contador PASS/FAIL de test-brain.sh se perdía en asertos dentro de subshells — un `bad` ahí imprimía FAIL pero el script cerraba en "0 FAIL" exit 0. Arreglado con un archivo de conteo (inmune a fork), verificado con 2 canarios inyectados (FAIL real → aparece + exit≠0). De paso: 7 tests nuevos para el hueco de cobertura que esa misma auditoría señaló (eval/bash-c en secret-scan+squash-guard+confirmar-merge, cross-repo de secret-scan con CLAUDE_PROJECT_DIR explícito) — los 5 guards YA estaban protegidos por M1 (verificado ejecutando contra el código pre-M1: SÍ evadían), solo faltaba el test. Suite: 1011 (roto)→1190 (contador arreglado)→1206 PASS · 0 FAIL, cada mecanismo verificado antes/después por ejecución en sandbox aislado (nunca el worktree del orquestador). PENDIENTE: CACHE-DE-CREACION (la mitad de M3 que cachea el --base/--target-branch al CREAR el MR) quedó [SIN CONFIRMAR] — requiere un nuevo hook PostToolUse cuyo schema exacto del payload (tool_response vs tool_output) no pude verificar con confianza (el propio repo documenta un nombre, una fuente externa consultada da otro); el lado LECTOR del caché ya está implementado y probado, listo para cuando se confirme el schema real.
- 2026-09-16 · guards-sustrato: los 2 CRÍTICOS + 2 ALTO de la auditoría FMEA de la unificación cerrados · claude(fixer) · rama `fix/guards-criticos-fmea` (de `fix/guards-sustrato-unico`, pusheada, SIN PR/merge — lo abre el orquestador). Dictamen `~/.claude/jobs/fc94c4e1/tmp/auditoria-final-guards-fmea.md`. CRÍTICO-1 (sustrato único = punto único de fallo que falla ABIERTO): reproducido en vivo (paréntesis sin cerrar en `acg_despoja_comillas` → los 5 guards morían con exit=1, no-bloqueante para Claude Code). Fix: `source` de la lib se prueba primero en un SUBSHELL (el crash queda aislado ahí); si truena, cada guard falla RUIDOSO/CERRADO según su propia política (git-branch-guard/merge-squash-guard/confirmar-merge-develop: deny + stderr; secret-scan/proteger-arbol: degradan a su propio fallback, que YA existía pero nunca se alcanzaba). Snippet idéntico en los 5. CRÍTICO-2/3 (M6 dejaba pasar un merge de destino DESCONOCIDO —posible main— con un grant vigente, SIN pasar por el juez ni el piso M5): confirmado por A/B contra develop (mismo caso: pre-M6 DENY correcto, con M6 exit=0 en silencio, juez mockeado a DENY nunca se llamó). Fix: el grant durable solo se consulta con destino CONFIRMADO `develop`; con destino desconocido SIEMPRE cae al juez (fail-safe DENY si tampoco es alcanzable — el comportamiento PRE-M6). ALTO-1 (M5 bloqueaba también merges LEGÍTIMOS a develop bajo fallo de red, el camino más común): el juez ahora declara `DESTINO_INFERIDO` cuando la consulta vino vacía; el piso de main NO aplica si esa inferencia fue explícitamente 'develop' (conversación inequívoca, sin main/release en juego) — precisión, no relajación: cualquier otra inferencia deja el piso intacto. ALTO-2 (sin jq, TODO push/merge quedaba bloqueado, incluida la propia mini-develop, sin carril): git-branch-guard ahora deja pasar un `git push` que nombra EXPLÍCITAMENTE ≥2 tokens (remoto+rama) sin que ninguno sea develop/main/master/HEAD (bare push y mención de base SIGUEN bloqueados); merge-squash-guard/confirmar-merge-develop no tienen precisión de texto posible (el destino sale de la API) → ganaron el escape auditado `CLAUDE_GIT_GUARD_SIN_JQ_PERSONAL=1` (mismo espíritu que `CLAUDE_SKIP_SECRET_SCAN`). MEDIO (M3 CACHE-DE-CREACION sin escritor, cualquier archivo plantado en /tmp se confiaba a ciegas): `acg__cache_creacion_es_mia` ahora exige mismo uid + permisos sin grupo/otros antes de confiar; la atadura completa (hash de session_id) queda para cuando se implemente el escritor (sigue [SIN CONFIRMAR]). BAJO (1205→1206 PASS/FAIL no determinista en la corrida anterior): INVESTIGADO — reproducido un mecanismo real de no-determinismo: bajo el Bash tool de Claude Code, `grep` puede estar cableado como función de shell exportada, y el patrón `_p="$(command -v grep)"; ln -s "$_p" ...` que el arnés usaba en 4 sitios creaba un symlink "grep -> grep" autorreferenciado (el binario "desaparecía" del PATH restringido de las pruebas sin-jq). Nuevo helper `_mkbin_real` (resuelve SIEMPRE contra /usr/bin·/bin·/usr/local/bin·/opt/homebrew/bin, nunca `command -v`) reemplaza los 4 sitios. No se puede afirmar con certeza que ESTA fue la causa exacta del FAIL original (no se capturó qué test falló), pero es una fuente de flakiness real, reproducida y cerrada. Suite: 1206→1232 PASS · 0 FAIL (26 tests nuevos, uno por hallazgo — cada uno falla contra el código viejo/pasa contra el nuevo, verificado por ejecución), 4 corridas consecutivas limpias tras el fix de determinismo. Doc actualizada en la misma tanda (MEMORY.md: las 5 secciones de guard tocadas). PENDIENTE: nada de este dictamen quedó sin atender (los 2 CRÍTICO + 2 ALTO tienen fix+test; MEDIO se evaluó y se hizo el mínimo defendible; BAJO se investigó y se corrigió lo reproducible).
- **[worktrees pendientes tras barrido]**
  - worktree `guard-merge-consolidado` (rama `fix/guard-merge-consolidado`, YA integrada a develop) retenido por 25 archivo(s) sin commitear/untracked — revisar y limpiar; mientras siga sucio CONGELA esa rama y nadie la barrerá.
  - worktree `guards-sustrato` (rama `fix/guards-sustrato-unico`) sin mergear a develop — retomar o cerrar.
  - worktree `pf` (rama `fix/rehidratar-visible-post-compact`, YA integrada a develop) retenido por 1 archivo(s) sin commitear/untracked — revisar y limpiar; mientras siga sucio CONGELA esa rama y nadie la barrerá.
  - worktree `aud-ejecucion` (rama `audit/ejecucion-guards-433`) sin mergear a develop — retomar o cerrar.
  - worktree `aud-semantica` (rama `audit/semantica-guards-433`) sin mergear a develop — retomar o cerrar.
  - worktree `aud-skills-obsol` (rama `audit/skills-obsolescencia`, YA integrada a develop) retenido por 1 archivo(s) sin commitear/untracked — revisar y limpiar; mientras siga sucio CONGELA esa rama y nadie la barrerá.
  - worktree `aud-skills-solape` (rama `audit/skills-solapamiento`) sin mergear a develop — retomar o cerrar.
  - worktree `aviso-contexto-util` (rama `fix/aviso-contexto-util`) sin mergear a develop — retomar o cerrar.
  - worktree `dedupe-firma` (rama `fix/dedupe-firma-canonica`) sin mergear a develop — retomar o cerrar.
  - worktree `despersonalizar-seeder` (rama `docs/seeder-sin-gustos-personales`) sin mergear a develop — retomar o cerrar.
  - worktree `dod-timeout` (rama `fix/dod-timeout-carrera`) sin mergear a develop — retomar o cerrar.
  - worktree `guards-fmea` (rama `fix/guards-criticos-fmea`) sin mergear a develop — retomar o cerrar.
  - worktree `integracion` (rama `integracion/cortex-2026-09-17`) sin mergear a develop — retomar o cerrar.
  - worktree `overhead` (rama `audit/overhead-harness`) sin mergear a develop — retomar o cerrar.
  - worktree `promesas` (rama `fix/promesas-sin-cableado`) sin mergear a develop — retomar o cerrar.
  - worktree `req-sesiones` (rama `docs/requerimientos-unificar-tooling-sesiones`) sin mergear a develop — retomar o cerrar.
  - worktree `scan-cota` (rama `fix/secret-scan-sin-cota`) sin mergear a develop — retomar o cerrar.
  - worktree `sk-coherencia` (rama `docs/auditor-coherencia-lecciones`) sin mergear a develop — retomar o cerrar.
  - worktree `sk-semantico` (rama `docs/auditor-semantico-lecciones`) sin mergear a develop — retomar o cerrar.
  - worktree `sk-suficiencia` (rama `docs/auditor-suficiencia-lecciones`) sin mergear a develop — retomar o cerrar.
  - worktree `skills-audit` (rama `docs/auditores-lecciones-wg`) sin mergear a develop — retomar o cerrar.
  - worktree `cablear-cerrar-slice` (rama `fix/cablear-cerrar-slice-gates`) sin mergear a develop — retomar o cerrar.
- 2026-09-18 · RELEASE `09796fd` (overhaul consolidación guardas/skills/hooks #438-441 + skill control-gui-remota-por-ssh #442 + recordar-cosechar sync+nudge #444 + DUPLA convergida 0 c/a/m #443/#445/#446, 43 commits). RELEASE `4ab102f` (3 fixes post-DUPLA: diente de revisión crítica de agentes en delegacion-registrar #448, widget deriva su roster del MANIFEST no-hardcode #450, recetas control-gui Linux/macOS #449). Widget QA-confirmado por unjordi (sin fantasmas tras rebuild). test-brain 1354/0. Auth permanente durable de merge-develop registrada (NO cubre gh pr merge).
- 2026-08-05 · PR #262 (`6614220`) · Juez de merge decide el destino + PISO DETERMINISTA de main (`confirmar-merge-develop.sh` infiere destino cuando `acg_destino_de_mr` viene vacío, fail-seguro duda+release→main; piso main+ALLOW sin lenguaje de release del USUARIO→DENY; transporte curl→api.anthropic.com OAuth ~1.3s). Baterías piso-main + LIVE 28 verdes.
- 2026-08-06 · release #265 (`ad0ad68`, v0.2.291) · Juez de MERGE EMPODERADO liberado a main (max_tokens 16→768, temp 0, CoT `VEREDICTO:`, veto de cita `CITA:` verbatim, hint PRs, piso barato, piso determinista main; triple-lever opt-in `CLAUDE_MERGE_JUEZ_VOTES`/`_TEMP`; modelo Haiku desamordazado). 467/0 determinista + 5/5 adversariales. 1er merge CLI real: plantilladotnet !114. Diseño en `[[juez-empoderamiento]]`.
- 2026-08-06 · #266 (`6f969c0`) · Juez del DoD EMPODERADO en develop (`dod-verificar.sh` max_tokens 32→512 temp 0, 3 centinelas `CIERRE:/MARCA:/VISUAL:`, veto de cita sobre MARCA, fail-OPEN preservado; batería `djlive`). **NOTA: el release develop→main #267 quedó pendiente (lo frenó un FP del merge-squash-guard) — REVISAR si ya se liberó antes de cerrar del todo.**
- 2026-09-03 · MR #351 (→develop `677d7ca`) · #78 RESUELTO — el hook heredaba el PATH mínimo de launchd (GUI) donde `jq` sí pero `gh`/`glab` NO (viven en /opt/homebrew/bin · /usr/local/bin) → consultas de destino por API vacías → fail-safe frenaba merges/releases legítimos. Fix: `acg__augmenta_path()` en la lib compartida añade los bin canónicos al FINAL del PATH (escape `ACG_PATH_AUGMENT=0`), con test anti-weakening.
- 2026-09-18 · #450 (released `4ab102f`) · widget-roster-desde-MANIFEST (era el "incompleto 5" fantasma) — QA-confirmado por unjordi.
- 2026-08-2x · release #319 (`bc83604`) develop→main · rename claude-brain→cortex + #315 construir-missing-manual · #316 como-trabajar barebones · #317 instalador barre "Claude Brain Widget" viejo (mata doble-widget) · #318 limpieza memoria. (#322 resolveClonePath fallback ~/.claude-brain + #323 updater renombra clon a ~/.cortex quedaron en develop, NO en main con este release.) + Cachy claude-cli 2.1.243 segfault→rollback 2.1.241 (ver `ecosistema-claude.md`).
- **[worktrees pendientes tras barrido]**
  - worktree `guards-fix` (rama `fix/guards-sustrato-unico`) sin mergear a develop — retomar o cerrar.
- **[rama represada]** `feat/term-broker` — 14 día(s) sin actividad, sin integrar, SIN PR. Decidir: abrir/retomar el MR/PR, mergear o descartar (NO se borra sola).
- 2026-09-22 · cortex-master(axon) · Triado el backlog-desarrollo.local.md del WIDGET (per-máquina Cachy): 5 ítems vivos fundidos al estado-proyecto.md canónico (carry-forward OAuth · extractor Chats · pestaña Cerebro reconciliar-slugs · bandeja KDE · plasmashell). El resto (Audit P0-P17 + 2 bloques de pain-points forenses jul + HECHO renombrar/mover sesión) ya estaba en develop/normas. Archivo local RETIRADO (contenido vivo preservado en el canónico que viaja por git).
