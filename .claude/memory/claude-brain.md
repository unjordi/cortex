---
name: claude-brain
description: "Widget KDE+macOS+Windows de límites de uso de Claude; repo propio github.com/unjordi/claude-brain (fork de fuziontech restyleado al look FelixDes)"
metadata: 
  node_type: memory
  type: project
  originSessionId: f59bc25a-fdde-4d83-80c0-f29da9699946
---

Widget de escritorio open-source que muestra los límites de uso de Claude ("Tus límites de uso" de claude.ai: sesión 5h + semanal).

**Ubicación canónica (desde 2026-06-30):** repo propio **`github.com/unjordi/claude-brain`** (fork público de fuziontech), clonado en `~/code/claude-brain`. Es la **fuente de verdad ÚNICA**: el código y el "cerebro" de Claude (memoria/skills en `.claude/`) viajan juntos por el repo. En otra máquina (la MacBook): clonar el repo y correr `bash .claude/bootstrap-claude.sh` una vez (enlaza la memoria al slug de esa máquina; las skills se autocargan). `origin` = tu fork; `upstream` = fuziontech (para jalar mejoras / eventual PR upstream). **Ya NO vive en `scripts/`** (se extrajo el 2026-06-30 para no duplicar ni dejar rastros que confundan). Antes era al revés: canónico en `scripts/` + Google Drive, y este clon estaba dormido.

**Decisión (2026-06-29):** forkear `github.com/fuziontech/claude-brain` (MIT, KDE Plasma 6 + app macOS Swift, muestra dinero API-equiv vía ccusage, lee endpoint read-only) y darle el LOOK del de la KDE Store `github.com/FelixDes/claude-kde-usage-widget` (más bonito pero solo KDE, sin dinero, y su método QUEMA cuota: hace una petición real por refresh). Base = fuziontech; FelixDes = referencia visual.

**Dato técnico clave:** los datos reales salen del endpoint OAuth `https://api.anthropic.com/api/oauth/usage` (el mismo de `/usage` de Claude Code), autenticando con el token de `~/.claude/.credentials.json`. fuziontech lo lee read-only (no gasta cuota). El % es exacto; el `$` es API-equivalente estimado por ccusage. NO es API pública documentada → puede romperse.

**Estado:** instalado y corriendo en CachyOS (`basis: oauth`, semanal 45% clavado con claude.ai). Popup tipo tarjeta "Claude Limits" + indicador de bandeja de 2 filas con mini-barras. Paleta: **naranja tipo FelixDes** (rojo solo >90%). Icono: **speedometer** (era el martillo `applications-development`, que salía feo en el selector de widgets). Instalado vía `kpackagetool6 -t Plasma/Applet -i/-u <path>/src/plasmoid` (no hay `just`; preview con `plasmoidviewer -a <path>` + spectacle, recargar con `kquitapp6 plasmashell && kstart plasmashell`). ccusage instalado global con pkexec (su binario nativo necesitó `chmod +x`).

**Rebrand a unjordi (PR #67, merged a develop):** Id plasmoide `io.github.unjordi.claude-brain`, bundle/launchd macOS `io.github.unjordi.claude-brain`. Swap de Id en vivo YA hecho (viejo `fuziontech` desinstalado, nuevo instalado) → unjordi debe RE-AGREGAR el widget (tray + escritorio). Authors=unjordi (sin email) + crédito fuziontech. LICENSE MIT original intacta.

**Popup de 3 pestañas (PR #69, 2026-06-29):** riel de pestañas vertical a la IZQUIERDA + StackLayout. (1) **Límites** (lo original). (2) **Resumen**: tarjetas (tokens, días activos, modelo favorito, racha actual/larga, costo) + heatmap tipo GitHub. (3) **Modelos**: barras apiladas por día (color por modelo) + tabla con in/out y %. Datos de un `stats.json` que el `fetch` arma con `ccusage daily --json --breakdown` (LOCAL). **Fase 2 del Resumen LISTA (PR #70):** Sesiones (nº de .jsonl), Mensajes (líneas user/assistant) y Hora pico (histograma de timestamps → hora local −6) se calculan de los transcripts crudos `~/.claude/projects/**/*.jsonl` con grep/awk (no jq, por los ~155 MB; corre ~4s). Resumen quedó en 9 tarjetas 3×3 + heatmap. Confirmado bello en vivo (las 3 tarjetas de fase 2 con datos reales).

**Gotchas de iteración en vivo (me costaron mucho preview):**
- `kpackagetool6 -u <pkg>` **NO reemplaza archivos si `KPlugin.Version` no cambió** (ni `-r`+`-i` fue fiable). Para actualizar el instalado al iterar: **`command cp -rf src/plasmoid/contents/. ~/.local/share/plasma/plasmoids/<id>/contents/`** + recargar plasmashell. Para release real, **bumpear la Version**.
- El **`cp` de unjordi está aliaseado a `cp -i`** → sin TTY responde "no" y no sobrescribe. Usar **`command cp`**.
- **`plasmoidviewer` cachea/ignora el default de propiedades** (ej. `currentTab`): mostraba siempre la pestaña 0. Verificar otras pestañas EN VIVO en el panel (clic), no con plasmoidviewer.

**Replicación multi-OS:** **macOS con PARIDAD COMPLETA (2026-07-04, PR #2 mergeado a main por unjordi — a Claude el clasificador le bloquea `gh pr merge` a main en este repo):** se portó todo lo del plasmoid a la app Swift de `macos/` — fetch genera `stats.json` (bloque idéntico al Linux; BSD grep/date OK), popover de 3 pestañas en español (riel izquierdo, StatCards 3×3, heatmap, apiladas por modelo), ítem de barra de 2 filas 5h/7d, paleta naranja/rojo>90%, y el botón refresh ahora dispara fetch real (antes solo releía cache). Instalada y verificada en vivo en la MacBook (`~/Applications/Claude Brain Widget.app` + launchd cada 5 min; token OAuth sale del **Keychain**, no hay `.credentials.json` en Mac; Node vía brew + ccusage global). Gotcha visual: el riel de pestañas partía "Resumen" en 2 líneas al ir en bold → riel a 132pt + `lineLimit(1)`. Icono de app propio: `macos/make-icon.sh` genera `AppIcon.icns` programáticamente (Swift/AppKit here-doc, gauge naranja sobre squircle grafito — nada de binarios en el repo; `make-app.sh` lo mete al bundle). **Windows con PARIDAD (2026-07-06, rama `feat/windows-port`):** app de bandeja **WinForms .NET 10** nueva en `windows/` (no hay upstream). A diferencia de Linux/mac (script bash en timer), la app **hace el fetch en C# ella misma** cada 5 min (piso 5.5 min) y escribe `%LOCALAPPDATA%\claude-brain\{state,stats}.json` (mismo schema). Endpoint OAuth vía `HttpClient` da los % (token de `%USERPROFILE%\.claude\.credentials.json`); los tokens/días/modelos/sesiones/mensajes/hora pico salen de **parsear los transcripts en C# puro** (`JsonDocument`) — **sin bash/jq/curl/Node**. El **$ API-equiv es lo ÚNICO que necesita Node+ccusage**; sin él sale "—" y todo lo demás funciona. Icono de bandeja = **2 mini-barras SIN número** (la bandeja de Windows es cuadrada, no de ancho variable; % y ⟳reset van al tooltip+popup). `pwsh -File windows/install.ps1` publica un **.exe self-contained single-file** (~110 MB) a `%LOCALAPPDATA%\Programs\ClaudeBrain` + autoarranque `HKCU\...\Run`. **BUILD en macOS (verificado 2026-07-15):** el proyecto `net10.0-windows` (WinForms) COMPILA en la MacBook con `dotnet build -p:EnableWindowsTargeting=true` → **0 warnings / 0 errores** (el .NET SDK cross-targetea; NO se necesita Windows para COMPILAR). **RUNTIME/UI necesita Windows REAL** — NO corre en mac (`--shot`/`DrawToBitmap` de WinForms es Windows-only): "instalado, corriendo, `basis=oauth` (5h 5% / 7d 42%), stats reales (8 sesiones, 1057 msgs, pico 16h, Opus 4.8), `--shot`" se verificó en la **VM de la OTRA compu** (+ laptop de Liora + QA visual de unjordi). ⚠️ **CORRECCIÓN (2026-07-15): el `dockur/windows` en Docker NUNCA jaló en ESTA MacBook** — no es la vía de nada. La regla real: **compilar = mac (EnableWindowsTargeting); runtime/UI QA = Windows real (otra compu / Chunito)**. Requisito de build: .NET 10 SDK. **Paridad con la 4ª pestaña Proyectos + fixes del MR #7 (2026-07-06, mismo día):** mergeé `develop` a la rama y porté a Windows la tab Proyectos (agrega por carpeta `~/.claude/projects/<slug>`; nombre bonito desde `~/.claude.json .projects`, slug = cwd con `[^a-zA-Z0-9]`→`-` —en Windows aplica a `\ / : .`, ej. `//host.lan/Data/...`→`--host-lan-Data-...`, `C:\Users\x`→`C--Users-x`), `account_email` en el footer (de `.oauthAccount.emailAddress`), reset heredado (conserva último snapshot bueno si OAuth cae), popup a 520×420. **CONFIRMADO el hallazgo pendiente de la nota de macOS:** el conteo por LÍNEA infla tokens — en la VM eran 236 líneas assistant vs 97 ids únicos → `total_tokens` bajó de 1.25M a 486k al deduplicar por `message.id`. En mac/Linux ccusage ya deduplica; en Windows yo derivo days/models/projects del transcript, así que el dedup por id es OBLIGATORIO ahí. "Mensajes" SÍ sigue contando líneas crudas (paridad con el fetch). Todo verificado con --shot (4 pestañas + nombres de proyecto reales).

**El "$ duplicado" NO es bug (verificado 2026-06-29):** five_hour y weekly mostraban el mismo `cost_usd` porque hoy es LUNES (inicio de semana) y el único uso de la semana == el bloque de 5h activo. ccusage weekly buckets: el semanal arranca bajo al inicio de la semana (= solo el bloque de 5h activo) y sube/se separa del bloque de 5h conforme avanza la semana. El código jala bien block vs weekly de fuentes distintas. **Matiz real (no arreglable):** con `basis=oauth` el % es exacto (todas las superficies) pero el `$` lo estima ccusage de transcripts LOCALES de esta máquina → puede subestimar. Idea pendiente: etiquetar `≈ $X (local)` para que no parezca roto.

**Indicador de bandeja (system tray) — 2 gotchas resueltos (2026-06-29):** (1) las mini-barras del pill compacto COLAPSABAN a ancho 0 en la bandeja → la barra necesita `Layout.minimumWidth` (no solo `preferredWidth`). (2) el widget se EMPALMABA con los íconos vecinos → la bandeja/panel reserva espacio de `Layout.minimumWidth/preferredWidth/maximumWidth` de la compactRepresentation, NO de `implicitWidth` solo; sin esos hints, el contenido ancho se desborda sobre el vecino. Etiqueta del $ quedó `(API equiv local)`. unjordi lo tiene EN LA BANDEJA (no en el panel) y así le gusta.

**Pendientes:** ~~verificar pill de 2 filas en vivo~~ (LISTO); ~~rebrandear metadata a unjordi~~ (LISTO); ~~crear fork en GitHub bajo unjordi~~ (LISTO 2026-06-30: `unjordi/claude-brain` público, este repo); ~~construir la versión de **Windows**~~ (LISTO 2026-07-06, rama `feat/windows-port`, WinForms .NET — falta PR/merge); eventual publicación en KDE Store. Si quiere paleta Nord en vez de naranja, ver [[kde-tema-opaco]] (la mecánica de propagación quedó en la skill `paridad-terminal` del repo scripts/, que NO viajó a este repo).

**Fix de 3 bugs en macOS (2026-07-05, integrado a `develop` el 2026-07-06 vía MR #7 `fix/macos-popover-y-tab-proyectos`; hasta entonces vivió como cambios SIN commitear en el working tree — nunca estuvo "en develop local"). ✅ QA VISUAL de unjordi confirmado 2026-07-06 en la MacBook: popover 520×420 sin recorte, footer con `jordi.serra@pind.mx`, reset semanal correcto (100%/4d):**
1. **Popover "desfasado"/chico:** `AppDelegate.swift` fijaba `popover.contentSize = NSSize(520, 340)` mientras `PopoverView.body` declaraba `.frame(width:520, height:340)` — con 8 modelos o heatmap largo el contenido real medía ~400-410pt, más que el frame, y se recortaba (pestaña Modelos sin margen superior, última fila pegada al borde; heatmap de Resumen cortado). Al subir el `.frame` de PopoverView a `height:420` SIN tocar `AppDelegate`, el `NSHostingController` (que auto-reporta `preferredContentSize` desde el `.frame()` de SwiftUI) empezó a pelear con el `contentSize` de 340 fijado en el popover, y el popover se re-dimensionó tras el primer show quedando **desfasado** (el riel mostraba solo "Resumen"/"Modelos", "Límites" y el título de la pestaña activa quedaban fuera de vista arriba). **Fix real:** los DOS tamaños deben coincidir siempre — `popover.contentSize` en `AppDelegate.swift` y `.frame()` en `PopoverView.swift` ahora son ambos `520×420`. Si se vuelve a tocar el alto de una pestaña, hay que actualizar los dos lugares juntos (no hay ScrollView; el frame es fijo, como en el original).
2. **Footer del tab Límites:** antes `"estimado local · ⟳ 5 min · act. hace 1min"` (poco claro, y sin decir de qué cuenta son los datos). Ahora `"{correo} · ⟳ 5 min · últ. act. hace: {mag}"` — el correo sale de `~/.claude.json` → `oauthAccount.emailAddress` (NO está en `.credentials.json`/Keychain, que solo trae el token OAuth; hay que leer el config propio de Claude Code). Nuevo campo `account_email` en `state.json`, decodificado en `Snapshot` (Swift). Si `account_email` es null (archivo ausente o cuenta sin email), cae al texto viejo "datos reales"/"estimado local" como fallback. `RelativeTime.compactReset()` (ya existía, la usaba el panel/bandeja) reemplaza a `.relative()` en el footer para no duplicar "hace hace".
3. **Reset semanal mintiendo "hoy":** en fallback `basis:"cost"` (endpoint OAuth caído/inalcanzable), el reset semanal SIEMPRE se adivinaba como "próximo lunes 00:00 UTC" — para esta cuenta el reset real es **viernes 5am hora local** (11:00 UTC), nada que ver con lunes. Con la cuenta en fallback un domingo, "próximo lunes" caía ~5h después → el widget decía "se restablece en 5h" cuando en realidad faltaban días. **Fix:** antes de sobreescribir `state.json`, el fetch lee el `resets_at` del run ANTERIOR *solo si ese run anterior tenía `basis:"oauth"`* (dato real confirmado) y solo si ese timestamp sigue en el futuro (comparación lexicográfica de ISO-8601 "Z", válida porque ordena igual que cronológicamente); si el endpoint vuelve a fallar, usa ese reset real heredado en vez de adivinar lunes. Aplica a `five_hour` y `weekly` igual, en `macos/bin/claude-brain-fetch` y `src/bin/claude-brain-fetch` (mismo bloque, mantenidos idénticos por convención). **Verificado en vivo:** el endpoint SÍ respondió en esta sesión (`basis:"oauth"`, weekly reset `2026-07-10T11:00:00Z` = viernes 5am CDMX exacto, igual a claude.ai).
- **Gotcha nuevo para anotar:** `popover.contentSize` (AppKit) y el `.frame()` de la vista SwiftUI raíz **son dos fuentes de verdad que deben coincidir a mano** — no hay binding automático fiable; si difieren, el `NSHostingController` puede re-negociar el tamaño después del primer render y el popover se ve "saltado"/mal alineado tras abrir.
- Pendiente pedido por unjordi (2026-07-05): **4ª pestaña "por proyecto/slug"** — mismo look que Modelos (barras apiladas + tabla) pero agrupado por carpeta de proyecto (`~/.claude/projects/<slug>/*.jsonl`) en vez de por modelo. ccusage NO tiene ese breakdown (`daily`/`session` no traen project path) → hay que calcularlo aparte, igual que Sesiones/Mensajes/Hora pico (grep/awk sobre los jsonl crudos, agrupando por el nombre de carpeta).

**4ª pestaña "Proyectos" — LISTA y QAeada (2026-07-05, mismo día; integrada a `develop` el 2026-07-06 en el mismo MR #7 `fix/macos-popover-y-tab-proyectos`; QA visual de unjordi OK 2026-07-06, pestaña visible en el riel):** agrega uso de Claude Code por carpeta de proyecto. Implementación (`macos/bin/claude-brain-fetch` y `src/bin/claude-brain-fetch`, "Fase 3"):
- Nombre bonito del proyecto: `~/.claude.json` → `.projects` es un mapa keyed por RUTA REAL (`/Users/unjordi/code/eskeduler`); se traduce a slug de Claude Code (reemplaza `/`→`-`) y se mapea a `basename(ruta)` vía jq. Si un proyecto ya no está en ese mapa (borrado/otra máquina), cae a una versión legible del slug crudo.
- Conteo de tokens por proyecto/día: grep+awk sobre los jsonl crudos de cada carpeta de proyecto (mismo dato que fase 2), agrupando por `message.id` (campo `"id":"msg_..."` dentro de `"message":{}`) — **NO por línea**.
- **Bug importante encontrado y corregido:** Claude Code escribe **una línea jsonl POR BLOQUE DE CONTENIDO** de un turno del asistente (thinking/tool_use/text pueden ser líneas separadas), y CADA línea repite el mismo `message.id` y el MISMO `usage` acumulado del turno completo → sumar tokens línea-por-línea sin dedupe **triplica** (o más) el conteo real. La fase 3 dedupea con un `seen[id]` en awk. **Esto probablemente también afecta al conteo de "Mensajes" de la pestaña Resumen** (fase 2, `MESSAGES=$(grep -rcE '"type":"(assistant|user)"' ...)`), que cuenta LÍNEAS, no turnos reales — pendiente de confirmar y arreglar si unjordi lo pide (no se tocó esta sesión, fuera de scope de lo pedido).
- **Zona horaria:** el timestamp crudo es UTC; ccusage agrupa `daily` por **día calendario LOCAL** (sin `-z` pasado, usa la del sistema). La fase 3 replica esto con un `shiftDay()` en awk (suma `$OFF` horas —ya calculado en fase 2 para Hora pico— y si se pasa de medianoche, ajusta la fecha ±1 día con tabla de días-por-mes/bisiesto). Sin esto, actividad de madrugada (UTC-6 en CDMX) se contaba en el día UTC equivocado.
- **Hallazgo tangencial (NO arreglado, ojo):** los totales de la pestaña Modelos/Resumen (`ccusage daily --json --breakdown`, sin acotar a un agente) **agregan TODOS los CLIs de IA que ccusage detecta en la máquina** (Claude Code, y si están instalados: Gemini CLI, Codex, OpenCode, Amp, Droid, etc. — confirmado: ese día el "Uso por modelo" mostró modelos Gemini junto a los de Claude). Por eso el total de `Proyectos` (que SOLO lee `~/.claude/projects`, o sea solo Claude Code) puede ser bastante menor al de `Modelos`/`Resumen` para el mismo día — **no es un bug de Proyectos**, es que Modelos/Resumen no están acotados a Claude. Si algún día se quiere que Modelos/Resumen sean Claude-only, ver si `ccusage claude daily` (subcomando específico, existe en el `--help`) da el mismo desglose sin mezclar agentes.
- Swift: `StatsProject`/`DayProject` en `QuotaModel.swift`, helper `projectColor()/projectHex()` (reusa `modelPalette`), tab en `PopoverView.swift` (rail: Límites/Resumen/Modelos/**Proyectos**, icono `folder`).
- **Las pestañas Resumen/Modelos/Proyectos ahora van en un `ScrollView` individual** (Límites NO — su footer sigue pineado al fondo con `Spacer`/`maxHeight:.infinity` sin scroll, tal cual el diseño original). Esto es APARTE del fix del bug #1 (popover fijo 520×420): el scroll es una capa extra de seguridad para cuando la lista de proyectos/modelos crece más de lo que cabe en 420pt — no cambia el tamaño negociado del popover.

**RESUELTO — cuenta duplicada/dos identidades (diagnosticado y estabilizado 2026-07-06):** el widget saltaba entre dos cuentas (lecturas erráticas, Sesión 5h llegó a 315.3%). **Causa raíz confirmada:** son **dos cuentas GENUINAMENTE distintas** compartiendo el MISMO item del llavero (`Claude Code-credentials`, `acct=unjordi`), que solo guarda un token a la vez:
- **Terminal (Claude Code CLI):** `jordi.serra@pind.mx` — accountUuid `0f969ded-01cc-48cc-9ad8-975364ea35b6`, org PindDevelopment.
- **Claude.app (desktop):** `informatica@pind.mx` — accountUuid `25545a28-3d4f-4c72-ba10-be8d512883a2`. El desktop **trae su propio Claude Code integrado** (`~/Library/Application Support/Claude/claude-code/<ver>`) que escribe el token OAuth en el MISMO item del llavero → quien escribe de último gana → el widget mostraba una u otra según el último login. (El Claude.app en sí se autentica con **cookies de sesión web de claude.ai** cifradas, no con el token OAuth; ese token compartido del llavero es lo único en formato compatible con el endpoint `/api/oauth/usage` del widget.)
- **Cómo se identificó sin escanear el llavero a ciegas:** comparar `oauthAccount.accountUuid` de `~/.claude.json` (CLI) contra `lastKnownAccountUuid` de `~/Library/Application Support/Claude/config.json` (desktop) — dieron UUIDs distintos. El correo del desktop se supo tras alinear el login.
- **Solución elegida por unjordi (opción "alinear login" sobre "fijar UUID" o "leer cookie web"):** hizo `claude logout` + `claude login` en la terminal eligiendo la cuenta de **Claude.app** (`informatica@pind.mx`). Ahora terminal y desktop coinciden en `informatica@pind.mx` → sin flip-flop, widget estable en esa cuenta. **Verificado técnicamente:** `~/.claude.json` accountUuid = `25545a28…`; fetch corrió `basis:"oauth"` con `account_email:"informatica@pind.mx"` (five_hour 26%, weekly 45%). **✅ LISTO — QA visual de unjordi OK (2026-07-06):** captura en vivo mostró el footer con `informatica@pind.mx`, Sesión 5h 51% / Semanal 48%, y el indicador de bandeja de 2 filas reflejando la cuenta correcta. Flip-flop resuelto de punta a punta.
- **Fragilidad conocida:** si algún día se vuelve a `claude login` con `jordi.serra@pind.mx` en la terminal, el widget vuelve a saltar (mismo cajón del llavero) — pero se **vería** en el footer (que muestra la cuenta activa, red de seguridad del fix del 2026-07-05).
- **Blindaje IMPLEMENTADO en las 3 plataformas (2026-07-06, rama `feat/account-guard`):** opt-in. El usuario "fija" una cuenta esperada (uuid o email) en un archivo de config; si la cuenta activa (de `~/.claude.json .oauthAccount`) difiere, el fetch marca `account_mismatch:true` en state.json y la UI avisa en ROJO ("⚠ {correo} no es la cuenta fijada" en el footer + "⚠ otra cuenta" en el tooltip). Archivo de pin: Linux/mac `~/.config/claude-brain/account` (override `$CLAUDE_BRAIN_ACCOUNT`), Windows `%LOCALAPPDATA%\claude-brain\account`. **Windows** tiene menú de bandeja "Fijar esta cuenta" (escribe el accountUuid activo) / "Quitar cuenta fijada" — verificado en vivo (pin equivocado → footer rojo). mac/KDE leen el mismo archivo (editar a mano; sin menú aún). Campos nuevos en state.json: `account_uuid`, `account_mismatch` (los 3 fetchers: `windows/.../QuotaService.cs`, `macos/bin/` y `src/bin/claude-brain-fetch` — bash idéntico entre los dos últimos). Sin pin configurado ⇒ sin cambios (comportamiento previo).

**2026-07 — el repo se volvió `claude-brain` + pestaña "Cerebro" viva + gobernanza + autoupdate.** El repo `claude-brain` se **renombró a `github.com/unjordi/claude-brain`** (instalador maestro + casa canónica del CEREBRO GLOBAL compartible; ver dashboard global z58/z65). Novedades en las **3 GUIs** (paridad macOS/Linux/Windows):
- **5ª pestaña "Cerebro"** — infografía del cerebro global jerarquizada de 🔒 INVIOLABLE → 💡 SKILLS (árbol con espinas de color y conectores). Es **interactiva** (tocar una pieza → evento + ejemplo) y se **auto-refleja**: lee tu `~/.claude` REAL (hooks presentes+cableados en settings.json, normas por marcador/contenido, skills) y pinta el estado por pieza. Cara al usuario **BINARIA** (verde=bien / rojo=faltante, sin leyenda; la precisión de 4 estados vive en el detalle al tocar). **Curita 🩹 self-healing**: si falta algo, un botón corre el `install-brain.sh` empaquetado en la app y re-lee. Inspector: `BrainInspector.swift`/`.cs` + `brain-scan.sh` (QML). Contenido del catálogo curado en `brainTiers` de cada GUI (mantenerlo a mano; ver skill `agregar-hook-cerebro`).
- **Gobernanza nueva** (`brain/hooks/`, tier global): `secret-scan` (bloquea secretos en commit/push), `rama-vieja` (avisa base vieja), `limite-gasto` (freno duro de delegación por gasto). `test-brain.sh` → 45 PASS.
- **Rediseño visual del widget de Windows** (riel con píldora de acento, barras cápsula con degradado, tarjetas, base oscura elegante) — acercándolo al de macOS.
- **Autoupdate ligero (winturbo-style):** cada GUI embebe `version.json` (sha+fecha+repo) al buildear/instalar; consulta `commits/main` de GitHub y, si el repo avanzó, ofrece un banner "Actualizar widget" que hace **ff a origin/main + reinstala** (fail-open; nunca te deja sin widget). Solo es real desde un release a main con el clon limpio. Ver skills `agregar-hook-cerebro` y `publicar-widget`. **ONE-STOP en las 3 plataformas (paridad, 2026-07-23):** el instalador que re-corre el botón es el COMPLETO (cerebro + widget) — `install.sh` en Mac/Linux, e `install.ps1` en Windows instala el cerebro por defecto; antes el Mac pasaba `--no-brain` y en Windows tanto `install.ps1` como la ruta de descarga del updater eran solo-widget (se corrigió: la ruta de descarga corre el `install-brain.ps1` empaquetado). Un clic = máquina completa. El botón "🩹 Curar cerebro global" queda como self-heal SIN git pull. Detalle: `docs/autoupdate.md`.

**2026-07-11 — features de sesión + rebrand de nombre a "Claude Brain" + ícono de cerebro (EN PREVIEW en develop / rama del ícono; QA visual parcial de unjordi).** Jornada grande. Tres bloques:

- **Features de sesión (#104/#106, en develop):** el dropdown de sesiones de la pestaña Proyectos gana (a) **renombrar con contexto**: el diálogo muestra un `summary` DERIVADO (la petición inicial de la sesión, ≤320 chars) + botón **"Sugerir nombre"** opt-in que hace shell-out a `claude -p --no-session-persistence` (¡la bandera es CLAVE: sin ella cada sugerencia creaba una sesión fantasma en el slug `/` con cwd=/ que consumía cuota!). El `summary` **salta saludos puros/marcadores `[…]`/`<command-message>`** y concatena los primeros mensajes CON SUSTANCIA — si no, con el estilo "holaaaaaa" de unjordi salía "charla sin rumbo". Foundation en `bin/sessions-extract.js` (campos nuevos `summary`+`slug`). (b) **Mover sesión entre slugs**: `bin/session-move.js` (nuevo) mueve el `.jsonl` al dir del proyecto destino, RESPALDA en `~/.claude/session-move-backups/` y reescribe el `cwd` interno (para que `claude --resume` reanude coherente); los tokens se re-atribuyen solos (el fetch agrega por dir). Los 3 instaladores despliegan `session-move.js`. **Refresh tras mover/renombrar**: NO usar el fetch completo (lento/guardado a "uno a la vez") — re-correr SOLO `sessions-extract.js` y recargar la lista al instante (macOS `QuotaModel.refreshSessions`, Linux `sessionsExtractSource`, Windows `RefreshSessionsAsync`).

- **Rebrand `claude-brain` → "Claude Brain" (#107, PR abierto, con MIGRACIÓN).** El repo GitHub ya era `claude-brain`; esto alinea lo VISIBLE + rutas de runtime. **Criterio (importante para futuros cambios):** se RENOMBRA lo visible/runtime — `.app` "Claude Brain Widget", daemon `claude-brain-fetch`, launchd label `io.github.unjordi.claude-brain`, systemd `claude-brain.{service,timer}`, cache `~/{Library/Caches,.cache}/claude-brain` (y `%LOCALAPPDATA%\claude-brain`), logs `/tmp/claude-brain.*`. Se CONSERVA lo invisible/que rompería: **dir de config `~/.config/claude-brain`** (ahí viven `limits.env`/`machine-id`/`account` = calibración + identidad de sync; moverlo arriesga perderlos), **env vars `CLAUDE_BRAIN_ACCOUNT`/`_SYNC_DIR`** y **User-Agent `claude-brain`** (contratos cross-plataforma), carpeta de nube **`claude-brain-sync`** (ya branded), **namespace/target `ClaudeBrain`** (C#/Swift), **Id del plasmoid** (renombrarlo hace desaparecer el applet del panel), y el **nombre del repo/dir `claude-brain`**. **Migración** en cada instalador (idempotente): baja el daemon viejo (launchd/systemd), borra la app vieja, MUEVE el cache viejo→nuevo (config se queda quieto). Gotcha: el fan-out de 3 agentes divergió en config/env/User-Agent → hubo que reconciliar a mano (Linux se pasó de rosca).

- **Ícono "cerebro + chispa de Claude" (#108, stacked sobre #107).** Reemplaza el medidor (era "Claude Brain Widget"). **Fuente ÚNICA = SVG** en `assets/icon.svg` (arte grande, surcos + chispa fina) + `assets/icon-small.svg` (cerebro simple + chispa gruesa para **≤32px**, que a 16px lee nítido en el login item en vez de un blob). Render: **rsvg-convert** (librsvg, agregado a prereqs de macOS) → `.icns` (`macos/make-icon.sh` + `iconutil`), plasmoid (`src/plasmoid/contents/icons/claude-brain.svg` + metadata `Icon: "claude-brain"`), Windows `.ico` (packer .ico propio con python, 6 tamaños PNG — no hay ImageMagick). **Login item del daemon** (script pelón → macOS le pone "exec"): se le incrusta el ícono como ícono CUSTOM del archivo vía **`NSWorkspace.setIcon`** (`macos/set-icon.swift`) — **NO uses Rez/SetFile**, dejan el resource fork a medias (probado: 286 bytes rotos vs 214KB con setIcon). **GOTCHA CLAVE:** `make-app.sh`/el paso de fetch-icon deben **regenerar SIEMPRE el `.icns` desde el SVG**, NO "solo si falta" — un `.icns` rancio (el medidor de una versión previa) se quedaba pegado y se instalaba el ícono viejo (bug real que QAeó unjordi: el login item salía medidor). **QA visual de unjordi: login item = cerebro CONFIRMADO en vivo** (2026-07-11); app/Dock rebuildeados con cerebro; Linux/Windows pendientes de su reinstalada. Ver skill `cambiar-icono`.

**Flujo de QA de rama (nuevo, #105):** `bootstrap` acepta `CLAUDE_BRAIN_REF` → instalar una rama concreta sin merge: `curl …/develop/bootstrap.sh | CLAUDE_BRAIN_REF=<rama> bash` (Win: `$env:CLAUDE_BRAIN_REF='<rama>'; irm …/develop/bootstrap.ps1 | iex`). unjordi QAea así antes de mergear a develop/main.

**GOTCHA (2026-07-15) — el botón "Actualizar widget" ensucia `develop` local si lo corres estando en `develop`.** QA en CachyOS del mecanismo real (`git fetch origin --quiet && git merge --ff-only origin/main && bash install.sh`, el mismo que dispara el banner de la pestaña Cerebro): funciona bien (timer/plasmoid/cache quedan correctos), pero el `merge --ff-only origin/main` mueve el puntero de la rama ACTIVA a lo que sea que apunte `origin/main` — si esa rama activa es `develop` (caso del dev, no del usuario final), el `ff-only` empalma en `develop` local los **merge-commits de TODOS los releases `develop→main` anteriores** (23 en este caso, PR #23…#136), que nunca existieron en la historia de `develop`. El contenido queda idéntico (diff de árbol vacío), pero `git status` marca `develop` "adelante N" de `origin/develop` — parece trabajo sin pushear y no lo es. **Fix aplicado:** `git reset --hard origin/develop` (seguro aquí porque el diff de árbol era vacío — verificar ANTES de resetear en cualquier otro caso). **Por qué no es realmente un bug:** el mecanismo se diseñó para el usuario final, que vive en `main` (ver nota de arriba: "el autoupdate solo es real desde un release a main"); un dev QAeando el botón mientras vive en `develop` está fuera del caso de uso previsto — pero vale la pena saberlo ANTES de tocar ese botón en un clon de desarrollo. Si se quiere blindar el mecanismo mismo, la opción sería que el updater detecte la rama activa y solo haga `ff-only` si es `main` (o compare contra `origin/<rama-activa>` en vez de `origin/main` a secas).

**PR #138 — clon default del bootstrap oculto en las 3 plataformas (mismo día).** El one-liner clonaba visible (`~/claude-brain` en Linux/mac, `%USERPROFILE%\claude-brain` en Windows) — de ahí salió el duplicado huérfano que motivó todo lo anterior. No se puede simplemente BORRAR el clon tras instalar (el autoupdate lo necesita), así que se oculta: `bootstrap.sh` → `~/.claude-brain` (con migración automática de un clon viejo visible vía `mv`), `bootstrap.ps1` → `%LOCALAPPDATA%\claude-brain-repo` (nombre `-repo` para no chocar con `%LOCALAPPDATA%\claude-brain`, el cache del daemon, ni con `%LOCALAPPDATA%\Programs\ClaudeBrain`, la app instalada; misma migración vía `Move-Item`). `brain-scan.sh` (curita self-healing) conoce la ruta nueva. READMEs de macOS/Linux/Windows actualizados.

**DECISIÓN DURA (2026-07-26) — cada release a `main` reconstruye TODOS los assets precompilados de TODAS las plataformas.** REGLA: el escenario "`main` avanza sin reconstruir sus assets rolling" es IMPOSIBLE por política. Se **quitó el filtro `paths:`** del trigger `on: push: branches:[main]` en `release-macos.yml` y `release-windows.yml` → CUALQUIER push a `main` dispara reconstruir+publicar el asset de esa plataforma (`macos-latest` / `windows-latest`). **PORQUÉ:** los workflows estaban path-filtrados (`paths: macos/**|brain/**|assets/**` y `windows/**`); los últimos releases a `main` no tocaron esos paths → el CI NUNCA reconstruyó los assets rolling y quedaron RANCIOS mientras `main` avanzaba a `59ea569`: el asset de `macos-latest` tenía build-sha **997e596** (viejo) y el de `windows-latest` **673a330** (¡dos releases atrás!). El auto-update DESCARGA ese asset viejo pero CHEQUEA contra `commits/main` → re-descarga el mismo `.app`/`.exe` viejo para siempre = **loop infinito de "actualiza"**. **COSTO ACOTADO:** el **dedupe-guard** del runner macOS (step "¿El .app de este commit ya está publicado?" — lee `build-sha:` del release rolling y salta compilación si coincide con `github.sha`) evita recompilar en el runner caro cuando el asset de ese sha ya existe; se **conservó** intacto. **Windows OJO:** `release-windows.yml` NO tiene ese dedupe-guard hoy → sin filtro de paths, recompila el `.exe` en cada push a `main` (build .NET, barato comparado con el runner macOS). Si se quisiera acotar igual, añadir un guard análogo (pendiente, no urgente). **Linux:** es plasmoid QML sin compilar → NO tiene asset precompilado (autoupdate git-based); no necesita workflow de release (confirmado: solo existen esos 2 release workflows + `ci.yml` de autovalidación).
