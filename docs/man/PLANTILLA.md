# <comando>

**Sinopsis:** `<cómo invocarlo, con sus flags reales>`

## Qué hace
<1-3 líneas: el propósito real, leído del script — no inventado.>

## Opciones / argumentos
| flag / arg | qué hace |
|---|---|
| `--x` | … |
<solo los que el script REALMENTE parsea (usage()/getopts/process.argv/case). Si no acepta ninguno, dilo.>

## Ejemplos
```bash
<1-2 invocaciones reales típicas>
```

## Notas
<gotchas reales del script: requiere root, escribe dónde, efectos irreversibles. Omite si no hay.>
