<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Cortex Widget — Windows tray port.
    Self-contained WinForms app: no bash/jq/curl needed. It reads the OAuth
    token from %USERPROFILE%\.claude\.credentials.json, calls the same
    /usage endpoint the other ports use, parses the local transcripts for the
    stats tabs, and (optionally) shells out to ccusage for the API-equiv $.
  -->
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <ApplicationHighDpiMode>PerMonitorV2</ApplicationHighDpiMode>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <!-- Nombre del EXE (Cortex.exe). El namespace interno se queda en Cortex
         a propósito: renombrarlo sería churn masivo sin valor visible. -->
    <AssemblyName>Cortex</AssemblyName>
    <RootNamespace>Cortex</RootNamespace>
    <!-- Ícono del exe (Explorer/taskbar): la ruleta FelixDes, generado del mismo master que
         el AppIcon de macOS. Sin esto Windows muestra el genérico de .NET. -->
    <ApplicationIcon>Cortex.ico</ApplicationIcon>
    <Version>1.0.0</Version>
    <Product>Cortex Widget</Product>
    <Company>io.github.unjordi</Company>
    <!-- Single-file / RID are passed by install.ps1 at publish time so that
         plain `dotnet build` / `dotnet run` stays simple during development. -->
  </PropertyGroup>

</Project>
